# Readio Cloud UI - Development Environment Configuration
# Note: Most configurations are fetched from the backend via /api/v1/config at runtime.

# API Base URL (Point to your local Go backend)
VITE_API_BASE_URL="http://localhost:8080"

