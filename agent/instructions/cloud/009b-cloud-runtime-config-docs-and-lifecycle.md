# Instruction 009b: Cloud Runtime Config Docs And Lifecycle [COMPLETED]

## Parent

This is the second execution step for:

- `009-cloud-runtime-config-ownership-cutover.md`

Do not start this instruction until `009a-cloud-runtime-config-code-and-tests.md` is reviewer-approved.

## Objective

Close the documentation and lifecycle portion of the Cloud runtime config ownership cutover after the code contract is stable.

## Required Work

### 1. Sync Cloud docs

Update the task-relevant Cloud docs to state clearly:

- production Cloud `/env.js` is backend-generated by `apps/cloud-api`
- `apps/cloud-ui/public/env.js` is dev/default-template oriented
- server-owned runtime config belongs in VPS env/systemd env files
- browser-public runtime config is emitted by backend allowlist, not dumped from process env

Update zh counterparts where present.

### 2. Sync Cloud handoff docs

Update the correct Cloud handoff sub-docs to record:

- the runtime config ownership buckets
- the production `/env.js` contract
- the distinction between browser-public runtime config and browser-local/user-local config

Do not put implementation detail into the handoff index beyond summary/map updates.

### 3. Add a decision-log entry

Record the architectural decision that Cloud production runtime config ownership moved to backend-generated `/env.js`, including:

- alternatives considered
- why static front-end runtime files are no longer the production source of truth
- risk notes about browser-public fields not being secrets

Update zh counterpart if present.

### 4. Close instruction lifecycle

After code and docs are complete:

- mark `009a` `[COMPLETED]` if not already done
- mark this instruction `[COMPLETED]`
- update the parent `009` lifecycle state
- add `Completion` sections with `Completed by`, `Commands`, and `Date`
- after reviewer approval, add `Reviewed by`

### 5. Roadmap sync

After reviewer approval only, update:

- `apps/docs/content/docs/general/technical-roadmap.mdx`
- `apps/docs/content/docs/general/technical-roadmap.zh.mdx`

Do not update the roadmap before reviewer approval.

## Verification

Run at minimum:

1. `git diff --check`
2. any focused doc/runtime verification needed to confirm paths, variable names, and config keys exactly match the codebase

## Done When

- Cloud docs and handoff docs match the implemented runtime ownership model
- decision log records the ownership cutover decision
- parent and child instruction lifecycle state is internally consistent
- roadmap is updated only after reviewer approval

## Completion

Completed by: Worker
Reviewed by: Reviewer
Date: 2026-03-31

Commands run:
- Task 1: Updated apps/docs/content/docs/apps/cloud/README.mdx with Runtime Config Ownership section
- Task 1: Updated apps/docs/content/docs/apps/cloud/README.zh.mdx
- Task 1: Updated apps/docs/content/docs/apps/cloud/deployment.mdx with Section 0a Production Runtime Config Contract
- Task 1: Updated apps/docs/content/docs/apps/cloud/deployment.zh.mdx
- Task 2: Updated apps/docs/content/docs/apps/cloud/handoff/index.mdx with Section 8 Runtime Config Ownership Audit
- Task 2: Updated apps/docs/content/docs/apps/cloud/handoff/index.zh.mdx
- Task 3: Added decision-log entry D118 in apps/docs/content/docs/general/decision-log.mdx
- Task 3: Added decision-log entries D117 and D118 in apps/docs/content/docs/general/decision-log.zh.mdx
- Task 4: Updated agent/instructions/cloud/009b-cloud-runtime-config-docs-and-lifecycle.md with [COMPLETED] and Completion section
- Task 4: Updated agent/instructions/cloud/009-cloud-runtime-config-ownership-cutover.md with Completion section

Roadmap sync: DEFERRED (awaiting reviewer approval)
